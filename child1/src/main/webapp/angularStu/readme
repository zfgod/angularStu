用户列表： 数据展示 == 1
        搜索条件
              输入框字符、int 1
                 下拉框选择

         分页 == 1
         列表单条数据操作 == 1  》》 查看
         列表单条数据部分参数提交 == 0
         列表批量数据提交 == 0
         没有数据时的处理 == 0

页面跳转+携带参数：
        　<a href=""> 标签： 1
          js请求成功或者失败跳转 ：window.location.href='' 1，
                               location.replace('')：没有记住历史功能
          路径问题：1
用户查看：
         页面初始化静态内容完成加载数据 == 1
         form表单填充数据+双向数据绑定 == 1
         json/form 提交表单 == 1
参数处理：
         表单数据校验： 必填，影藏编辑，数据内容格式 == 0
         表单提交校验 == 0
      filter自定义
         表单数据格式化+提交格式化 ： date日期,   1
         3元运算展示内容  == 0
         表单-条件展示内容 == 0
         int类型限制+parse == 1

弹窗提示     == 1
弹窗显示内容 == 0
回送数据换行 == 0

